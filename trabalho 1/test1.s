tipo:
   integer = inteiro

função:
   somar( valor integer:x , valor integer:y ): integer =
      ação: retorne x + y

   subtrair( valor integer:x , valor integer:y ): integer =
      ação: retorne x - y

   dividir( valor integer:x , valor integer:y ): integer =
      ação: retorne x / y

   multiplicar( valor integer:x , valor integer:y ): integer =
      ação: retorne x * y

ação:
   somar(1, 1);
   subtrair(1 , 1);
   dividir(2, 1);
   multiplicar(2 , 1)

